// Separated from icons.ts — Next.js 14 SWC cannot resolve a const export
// from a module that also has re-exports from external packages.
export const ICON_DEFAULTS = {
  className: "w-4 h-4 shrink-0",
  strokeWidth: 1.5,
} as const
